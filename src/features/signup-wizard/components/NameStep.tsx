import { StepPlaceholder } from "./StepPlaceholder";

export function NameStep() {
  return (
    <StepPlaceholder
      title="What should we call you?"
      description="Your name as it should appear on your profile."
    />
  );
}